#!/usr/bin/env bash
# skill-reminder.sh — UserPromptSubmit hook
# Injects a reminder about available governance skills to boost activation rate.
# Borrowed from HackerNoon governance layer pattern.

echo "Available governance skills: /preflight (gather context before coding), /qc (review changes), /task-doc (track work), /close-task (finalize), /evolve (update governance). Consider whether any should run for this task."
exit 0
