# Test Pattern
<!-- Generated by Clauding Thought /init from sampling existing tests -->

## Canonical Example

```{{language}}
{{canonical_test}}
```

## Test Structure

- Framework: `{{testing_framework}}`
- Location: `{{test_directory}}`
- Feature tests: `{{feature_test_path}}`
- Unit tests: `{{unit_test_path}}`

## Data Setup

- Approach: `{{test_data_approach}}`
- Factory usage:
```{{language}}
{{factory_example}}
```

## Authentication in Tests

```{{language}}
{{test_auth_example}}
```

## Assertions

Preferred assertion style:
```{{language}}
{{assertion_example}}
```

## Database Handling

- Trait/approach: `{{db_trait}}`
- Seeding requirements: `{{seeding_notes}}`

## What to Test

Every change should have tests covering:
1. Happy path -- the expected behavior works
2. Authorization -- unauthorized users are rejected
3. Validation -- invalid input is rejected with correct errors
4. Edge cases -- boundary conditions, empty states

## Anti-Patterns

Do NOT:
{{#each test_antipatterns}}
- {{this}}
{{/each}}

## Sibling Reference Files

{{#each sibling_tests}}
- `{{path}}` -- {{description}}
{{/each}}
