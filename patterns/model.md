# Model Pattern
<!-- Generated by Clauding Thought /init from sampling existing models -->

## Canonical Example

```{{language}}
{{canonical_model}}
```

## Required Elements

Every model MUST have:
{{#each model_requirements}}
- {{this}}
{{/each}}

## Tenancy

<!-- Omit if no tenancy -->
- Trait: `{{tenancy_trait}}` from `{{trait_path}}`
- Scope column: `{{scope_column}}` in `$fillable`
- Migration: `$table->foreignId('{{scope_column}}')->constrained()->cascadeOnDelete()`

## Relationships

Convention: `{{relationship_convention}}`

```{{language}}
{{relationship_example}}
```

## Casts

Defined as: `{{casts_style}}`

```{{language}}
{{casts_example}}
```

## Anti-Patterns

Do NOT:
{{#each model_antipatterns}}
- {{this}}
{{/each}}

## Sibling Reference Files

{{#each sibling_models}}
- `{{path}}` -- {{description}}
{{/each}}
